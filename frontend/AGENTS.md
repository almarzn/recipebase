# Frontend

Angular 21 app with Zard UI + Tailwind CSS 4.

See `AGENTS.md` at the repo root for commands, architecture, and conventions.

Detailed frontend rules are in `.opencode/skills/` — load them when working on frontend code.
